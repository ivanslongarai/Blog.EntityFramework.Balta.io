// dotnet tool install --global dotnet-ef
// dotnet ef migrations add InitialCreation
// dotnet ef database update (connects to database and runs)
// dotnet ef migrations script -o ./script.sql (gets the script)
// dotnet ef migrations remove (remove the last one applied migration)
namespace BlogEFDatabaseMigration
{
    class Program
    {
        static void Main(string[] args)
        {
        }
    }
}
